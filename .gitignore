# Binaries
bin/
*.exe
*.dll
*.so
*.dylib

# Go
*.test
*.out
vendor/
coverage.out
coverage.html
*.coverprofile
*.prof
*.pprof
__debug_bin*
__debug_bin.exe*

# Air (live reload)
tmp/
build-errors.log

# Leftover from git filter-branch / filter-repo
.git-rewrite/

# IDE / OS
.idea/
.vscode/
.cursor/
.cursorignore
*.swp
*~
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db

# Local spreadsheets (not test fixtures)
*.xlsx
!**/testdata/*.xlsx

# App data: DB is local / rebuilt; live.json is overwritten by sync jobs
data/tga.sqlite
data/tga.sqlite-*
data/**/*.sqlite
data/**/*.sqlite-*
data/live.json
data/cache/

# Runtime live state / local fetch scratch (not data/live.json)
/live.json
/tmp_nascar_feed.json
/tmp_series_feed.json

# Local server logs
server_err.log
server_out.log

# Scratch PDFs / parse outputs for one-off data imports
agent-tools/

# Backup copies and extracts (not the canonical tree)
backups/

# Secrets and local overrides (keep .env.example in repo)
.env
.env.local
.env.*.local

# Optional: Node if you add tooling under the repo
node_modules/

# OS duplicate copies from manual downloads
* (1).jpg
* (1).jpeg
* (1).png
# Local i18n working exports (canonical: web/utils/*-ru.js, event JSON)
data/driver-names-full-en.txt
data/driver-names-full-ru-dict.txt
data/event_previews_ru.json
data/event_previews_ru/
