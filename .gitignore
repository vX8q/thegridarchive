# Build artifacts (Windows/Linux/macOS)
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Go
*.test
*.out
vendor/
coverage.out
coverage.html
*.coverprofile
*.prof
*.pprof
__debug_bin*
__debug_bin.exe*

# Air (live reload)
tmp/
build-errors.log

# Leftover from git filter-branch / filter-repo
.git-rewrite/

# IDE / OS
.idea/
.vscode/
.cursor/
.cursorignore
*.swp
*~
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db

# Local spreadsheets (not test fixtures)
*.xlsx
!**/testdata/*.xlsx

# Runtime DB (rebuilt from JSON on server start) + WAL sidecars
data/tga.sqlite
data/tga.sqlite-*
data/**/*.sqlite
data/**/*.sqlite-*

# Live state + downloaded image cache (regenerated at runtime)
data/live.json
data/cache/
/live.json
/tmp_nascar_feed.json
/tmp_series_feed.json

# Local server logs
server_err.log
server_out.log

# Local scratch + backup trees (never canonical)
agent-tools/
backups/
*.traineddata
/_gtwce*.json

# Secrets and local overrides (keep .env.example in repo)
.env
.env.local
.env.*.local
.env.example

# Optional: Node if you add tooling under the repo
node_modules/

# OS duplicate copies from manual downloads
* (1).jpg
* (1).jpeg
* (1).png
# Local i18n working exports (canonical: web/utils/*-ru.js, event JSON)
data/driver-names-full-en.txt
data/driver-names-full-ru-dict.txt
data/event_previews_ru.json
data/event_previews_ru/
